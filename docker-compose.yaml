services:
  minecraft:
    build: .
    container_name: mc-fabric
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "FABRIC"
      VERSION: "1.20.1"
    volumes:
      - .:/data
    restart: unless-stopped
